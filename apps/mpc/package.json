{
  "name": "@llama-wallet/apps-mpc",
  "version": "1.0.0",
  "type": "module",
  "main": "dist/index.html",
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  },
  "repository": {
    "type": "git",
    "url": "ssh://git@github.com/mpcooor/apps.git",
    "directory": "@llama-wallet/apps-mpc"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json && vite build",
    "encode:wasm": "bun scripts/wasm.ts",
    "encode:worker": "bun run build && bun scripts/worker.ts",
    "tscheck": "tsc -p tsconfig.json --noEmit"
  }
}
